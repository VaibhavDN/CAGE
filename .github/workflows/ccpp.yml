name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: configure
      run: sudo apt install libsfml-dev
    - name: Compile
      run: g++ View.cpp -o View -lsfml-system -lsfml-window -lsfml-graphics -lpthread
